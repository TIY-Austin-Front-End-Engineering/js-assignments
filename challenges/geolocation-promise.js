// Create function `getGeo()` that returns a Promise.
//
// This Promise will resolve to a coordinates object like: `{ lat: ..., long: ... }` if the `navigator.getGeolocation()` call is successful, and will reject with an error message if not.

function getGeo(){
    // ...
}